apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: calibre-book-db
  name: calibre-book-db
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 30Gi
  storageClassName: nfs
