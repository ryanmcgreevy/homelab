apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: calibre-web-db
  name: calibre-web-db
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: nfs